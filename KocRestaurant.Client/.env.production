# Production API Base URL
# By default, uses relative path '/api' assuming client and server are hosted on the same domain/IP.
# If your server is hosted on a separate domain/subdomain (e.g. api.kocrestaurant.com), change the value below:
# VITE_API_URL=https://api.kocrestaurant.com/api
VITE_API_URL=/api
